// export async function postItems(baseUrl: string, id: string) {
//   const url = `${baseUrl}/api/items&id=${id}`; // URL에는 쿼리 파라미터를 포함하지 않음

// import instance from "./axios";

// export async function PostItem(newItem: any, setResponse: any) {
//   try {
//     const id: string = "신민호";
//     const response = await instance.post(`${id}/items`, newItem);
//     console.log(response.data);
//     setResponse(response.data);
//   } catch {
//     console.log("에러");
//   }
// }
